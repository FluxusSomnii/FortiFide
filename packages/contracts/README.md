# @fides/contracts

Canonical domain types and runtime validation boundaries for the first runnable slice.

Rules:

- dependency-bottom
- no imports from any internal package
- no imports from app code
- small public API exported only from `@fides/contracts`
- canonical contract intent and JSON examples live in `CONTRACTS.md`
